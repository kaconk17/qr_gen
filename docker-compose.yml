version: '3.7'

services: 
  django_gunicorn:
    volumes: 
      - static_data:/staticfiles
    env_file:
      - .env
    build:
      context: .
    ports: 
      - "8000:8000"
    networks: 
      - main

volumes: 
  static_data:
networks: 
  main:
